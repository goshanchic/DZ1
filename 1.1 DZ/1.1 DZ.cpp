
#include <string>
#include <vector>
#include <algorithm> 
#include <iostream>
using namespace std;
int mass
int main() {
    int min1 = min(mass[0], mass[1]);
    int min2 = max(mass[0], mass[1]);
    for (int i = 2; i < n; i++)
    {
        if (mass[i] < min1)
        {
            min2 = min1;
            min1 = mass[i];
            continue;
        }
        if (mass[i] < min2)
            min2 = mass[i];
        count << min1 << endl
        count << min2 << endl
        return 0

    }
